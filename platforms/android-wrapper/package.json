{
  "name": "assurance-android-wrapper",
  "version": "0.31.0",
  "private": true,
  "description": "Android Studio wrapper for the Assurance renderer build.",
  "license": "MIT",
  "scripts": {
    "copy:web": "node scripts/copy-web-assets.mjs",
    "sync": "npm run copy:web && cap sync android",
    "open": "cap open android",
    "apk:debug": "npm run sync && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/core": "^8.4.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1"
  }
}
